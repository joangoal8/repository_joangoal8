package utils;

import java.util.HashMap;
import java.util.Map;

import objects.Cache;

public class HashMapCaches {

	public Map<Integer, Cache> mapCaches = new HashMap<Integer, Cache>();

	public Map<Integer, Cache> getMapCaches() {
		return mapCaches;
	}
	
}
